# Copy values into EasyPanel → project "nidhamauto" → each service → Environment
# Generate secrets: python3 -c "import secrets; print(secrets.token_hex(32))"

# ── db (PostgreSQL) ──────────────────────────────────────────────────────────
POSTGRES_USER=nidhamauto
POSTGRES_PASSWORD=CHANGE_ME_STRONG_DB_PASSWORD
POSTGRES_DB=nidhamauto

# ── backend ───────────────────────────────────────────────────────────────────
# Do NOT set DATABASE_URL if using POSTGRES_* (recommended).
POSTGRES_HOST=nidhamauto_db
POSTGRES_USER=nidhamauto
POSTGRES_PASSWORD=CHANGE_ME_STRONG_DB_PASSWORD
POSTGRES_DB=nidhamauto
POSTGRES_PORT=5432
FRONTEND_URL=https://nidhamauto.shop
SECRET_KEY=CHANGE_ME_RANDOM_64_HEX
ADMIN_USERNAME=admin
ADMIN_PASSWORD=CHANGE_ME_STRONG_ADMIN
ADMIN_JWT_SECRET=CHANGE_ME_RANDOM_64_HEX
GOOGLE_SHEETS_WEBHOOK_URL=
WHITELISTED_PHONES=+966550603
META_ACCESS_TOKEN=
META_PIXEL_ID=
TIKTOK_ACCESS_TOKEN=
TIKTOK_PIXEL_ID=
SNAP_ACCESS_TOKEN=
SNAP_PIXEL_ID=

# ── frontend ──────────────────────────────────────────────────────────────────
# CRITICAL for Traefik — without HOSTNAME=0.0.0.0 you get intermittent 502.
HOSTNAME=0.0.0.0
PORT=3000
BACKEND_INTERNAL_URL=http://nidhamauto_backend:8000
NEXT_PUBLIC_API_URL=https://api.nidhamauto.shop
NEXT_PUBLIC_ENABLE_PIXELS=true
NEXT_PUBLIC_META_PIXEL_ID=
NEXT_PUBLIC_TIKTOK_PIXEL_ID=
NEXT_PUBLIC_SNAP_PIXEL_ID=

# ── Domains (EasyPanel → Domain & Proxy) ─────────────────────────────────────
# frontend: nidhamauto.shop     → port 3000
# backend:  api.nidhamauto.shop → port 8000
